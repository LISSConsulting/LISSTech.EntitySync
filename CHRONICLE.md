> Project working memory for Ralph agents.
> Keep this file compact: unresolved state only, not a changelog. Completed history belongs in git, releases, and .ralph/logs/*.jsonl.

## Current Focus

- Active spec: `specs/001-lcat-sync-adapter/`.
- Spec tasks T001-T053 are complete. The current queue is empty; continue with the Empty Queue workflow unless a new spec task, blocker, or finding appears.
- Empty Queue sweep done: LCAT planning now blocks duplicate derived customer-scope slugs for review; `just test` passes.
- Empty Queue sweep done: LCAT adapter now rejects malformed customer-scope batch rows before HTTP send; `just test` passes.
- Empty Queue sweep done: LCAT apply and adapter paths now reject duplicate customer-scope slugs before HTTP send; `just test` passes.
- Empty Queue sweep done: LCAT apply and adapter paths now reject case-only duplicate N-central customer-scope IDs before HTTP send; `just test` passes.
- Empty Queue sweep done: LCAT planning now blocks non-NCentral source records before apply; `just test` passes.
- Empty Queue sweep done: LCAT adapter now trims customer-scope request values before validation and serialization, blocking whitespace-hidden duplicate N-central IDs before HTTP send; `just test` passes.
- Empty Queue sweep done: LCAT apply validation now compares trimmed approved customer-scope IDs/slugs before batch composition; `just test` passes.
- Empty Queue sweep done: LCAT slug fallback now sanitizes usable N-central IDs without allowing punctuation-only IDs through review safety; `just test` passes.
- Empty Queue sweep done: LCAT apply now returns a non-secret no-op result when a plan has no approved customer-scope items; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT adapter now reports malformed successful batch responses as redacted path-scoped errors without echoing raw response bodies; `just test` passes.
- Empty Queue sweep done: LCAT adapter now treats successful non-object batch responses as malformed redacted errors instead of zero-count successes; `just test` passes.
- Empty Queue sweep done: LCAT adapter now treats present but non-numeric batch count fields as malformed redacted errors; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT adapter now treats present but non-string audit event IDs as malformed redacted errors; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT adapter now treats negative batch response counts as malformed instead of reporting impossible sync totals; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT adapter now rejects null customer-scope batch rows before HTTP send; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT adapter now rejects empty customer-scope batches before HTTP send; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT apply now skips tampered approved plan items whose source is not an N-central Customer or Site before HTTP send; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: LCAT adapter duplicate-slug coverage now includes whitespace-hidden duplicates before HTTP send; `just test` passes.
- Empty Queue sweep done: LCAT response parsing now explicitly covers explicit-null and empty-string `audit_event_id` per the contract's "preserved when present" rule; `just test` passes.
- Empty Queue sweep done: LCAT response parsing now explicitly covers non-string `audit_event_id` shapes (number, boolean, object, array) at the unit level, mirroring the secret-leak integration guard; `just test` passes.
- Empty Queue sweep done: LCAT `TestConnectionAsync` now has Pester coverage for both the redacted transport-failure path and the non-success status path, closing the last unverified credential-leak vector in the connection test surface; `just test` passes.
- Empty Queue sweep done: LCAT adapter and apply command now share the `DefaultEntityMapper.IsValidLcatSlug` contract validator instead of carrying duplicate `LcatSlugPattern` regex fields, with a Pester guard that exercises the central validator against contract boundary cases; `just build`, `just test-load`, and `just test` pass.
- Empty Queue sweep done: dead `ConnectionRegistry.Names()` helper removed (no callers in `src/`, `Tests/`, `Module/`, or `docs/`); `just build` and `just test` pass (117 tests).
- Empty Queue sweep done: README "PowerShell API" table now lists all 12 exported cmdlets including `Set-EntitySyncCustomProperty` and `Invoke-EntitySyncNetSuiteSuiteQL`, and `docs/Connect-EntitySyncVendor.md` SYNTAX for `-Vendor HaloPSA` now advertises the 7 HaloPSA parameters actually exposed in code (`HaloNetSuiteCustomerIdFieldId`, `HaloCustomerRelationshipId`, `HaloCustomerRelationshipName`, `HaloCustomerTypeId`, `HaloCustomerTypeName`, `HaloAccountManagerEmail`, `HaloAccountManagerField`); `just test` passes (117 tests).
- Empty Queue sweep done: external help now covers every exported cmdlet — `docs/Get-EntitySyncConnection.md`, `docs/Test-EntitySyncConnection.md`, `docs/Invoke-EntitySyncNetSuiteSuiteQL.md`, `docs/Export-EntitySyncPlan.md`, and `docs/Import-EntitySyncPlan.md` close the 5-doc gap called out as a follow-up in `Module/LISSTech.EntitySync.psd1:12-25` vs `docs/`; `just build`, `just test-load`, and `just test` pass (117 tests).
- Empty Queue sweep done: `EntitySyncPlanArtifactSanitizer.IsSensitiveName` now uses identifier-boundary matching (not preceded by lowercase letter, not followed by lowercase letter or whitespace, with an end-of-string exception for trailing CamelCase credential components) instead of substring `Contains`, so English-phrase reviewer notes such as "tokenization policy review", "password reset pending", "secrets management policy", "bearer of good news", "reauthorization", and "credentialing" no longer get rewritten to `[credential redacted]`, while `LCATBearerToken`, `Authorization`, `BearerToken`, `NCentralRegistrationToken`, `password_reset`, and `LCATBearerToken=...` reason leaks still get redacted; Pester coverage locks in both directions and `just test` passes (118 tests).
- Empty Queue sweep done: HaloPSA and N-central `TestConnectionAsync` now have Pester coverage for the success path, the non-success path, and the transport-failure path (the same three vectors as the existing LCAT coverage); the transport-failure tests prove the bearer token and `Authorization` header never leak into the raised exception's message, and the success/non-success tests prove the bearer/user-api token is still sent on the wire when the host is reachable; a new `MultiShotHttpServer` helper queues canned responses so the N-central two-request authenticate-then-validate flow can be exercised in one test, and `just test` passes (124 tests).
- Empty Queue sweep done: HaloPSA rate-limit/backoff now has Pester coverage — `SendWithRateLimitAsync` is exercised end-to-end with `Retry-After: 1` (one 429 then 200, proving the bearer token rides each retry) and with seven consecutive 429s (proving the adapter stops after `MaxRateLimitRetries` = 6 and returns the final 429 while keeping the bearer on every retry), and the private `RateLimitDelay` helper is exercised via reflection across all three branches (`Retry-After` delta-seconds, future-date, past-date falling through to exponential) plus the `Math.Min(300, 15 * 2^attempt)` ladder from attempts 0..7; the `OneShotHttpServer` and `MultiShotHttpServer.ResponseSpec` test fixtures gained an optional `ExtraHeaders` collection so Retry-After can be sent without hand-rolling a parallel response writer, and `just test` passes (129 tests, +7s suite budget for the 6s exhaustion test).
- Empty Queue sweep done: NetSuite `TestConnectionAsync` now has Pester coverage for the success path (200 with OAuth header carrying `oauth_consumer_key` and `oauth_token`), the non-success path (401 returns `false` without leaking the OAuth credentials), and the transport-failure path (refused connection never echoes `ConsumerKey`, `TokenId`, `ConsumerSecret`, `TokenSecret`, or any `oauth_*` field in the raised exception's message); the existing pattern from HaloPSA/N-central is mirrored, requiring a small `NetSuiteOptions.BaseUrl` override (defaults to the canonical `https://{AccountId}.suitetalk.api.netsuite.com/...` URL when empty, preserving production behavior) so the adapter can be pointed at a local `OneShotHttpServer` without DNS tricks; `New-TestNetSuiteOptions` / `New-TestNetSuiteAdapter` helpers follow the LCAT/Halo/NCentral convention, and `just build` and `just test` pass (132 tests).
- Empty Queue sweep done: HaloPSA, N-central, and NetSuite `GetLookupsAsync` now have direct Pester coverage — HaloPSA covers `TopLevel` (`api/toplevel`), `CustomerRelationship` and `CustomerType` (both via `api/Lookup?lookupid=...` with the `LookupId` property round-trip), `NCentralIntegration` (`api/ncentraldetails?showall=true` with `Url`/`TopLevel`/`Username`/`EnableIntegrator`/`LastSyncDate`/`ServiceOrgId` parsing), and `NCentralIntegrationLink` (`api/ncentraldetails/{id}?includedetails=true&hasdisconnected=false` exercising both `client_links` and `site_links` so `SourceVendor`/`TargetVendor`/`Primary`/`ParentTargetId` mapping is locked); N-central covers `ServiceOrganization` with the authenticate-then-page sequence (`POST api/auth/authenticate` proves the user API token rides the wire, `GET api/service-orgs?pageNumber=1&pageSize=1000` proves the derived access token rides the next request) plus a transport-failure test that proves the user API token never leaks into the raised exception's message; NetSuite is locked to throw `NotSupportedException` for every supported lookup type so callers do not mistake "unimplemented" for "empty results"; each HaloPSA lookup test also asserts the bearer token rides the wire on the actual lookup URL, and `just build` and `just test` pass (140 tests).
- Empty Queue sweep done: `en-US/about_LISSTech.EntitySync.help.txt` (the module-level about-topic, copied to `Module/en-US/` during build via the `.csproj`'s `<None Include="..\en-US\*" CopyToOutputDirectory="PreserveNewest" />`) now documents all four vendors — LONG DESCRIPTION lists NetSuite customers, HaloPSA clients and sites, N-central customers and sites, and LCAT customer scopes; CONFIGURATION gains matching paragraphs for N-central (`NCENTRAL_BASE_URL`/`NCENTRAL_USERNAME`/`NCENTRAL_PASSWORD`/`NCENTRAL_REGISTRATION_TOKEN` with derived OAuth access token) and LCAT (`LCAT_BASE_URL`/`LCAT_BEARER_TOKEN` with `-LCATSecureBearer` support, target-only scope, bearer-token redaction guarantees, no-read-endpoint semantics, and the site-derived `ncentral_parent_customer_id` field); the existing HaloPSA paragraph is unchanged, the README already covers all four vendors so this closes the last operator-facing discoverability gap called out as a `tasks.md` T002 follow-up; verified via `Get-Help about_LISSTech.EntitySync -Full` after a fresh build, and `just test` passes (145 tests, 0 failed).
- Empty Queue sweep done: `Connect-EntitySyncVendor -Vendor LCAT` now accepts `-LCATSecureBearer <SecureString>` as a mutually-exclusive alternative to `-LCATBearerToken`, so an operator-session JWT minted by `LISSTech.DeviceAssetOps` (`Get-DeviceAssetOpsAccessToken`) can be reused without writing the cleartext token to a script transcript; the SecureString is unwrapped in-process via `Marshal.SecureStringToGlobalAllocUnicode`/`ZeroFreeGlobalAllocUnicode` and used only for the LCAT authorization header, never serialized into the returned connection or plan artifacts; mutual exclusivity is enforced via a single `InvalidOperationException`, the empty-SecureString guard rejects empty/whitespace-only cleartext with a non-leaking error, and `AddDynamicParameter<T>` keeps its single-parameter shape (the unused `parameterSetNames` overload added in the initial draft is dropped as dead code); Pester coverage locks in five vectors — happy-path `httpClient.DefaultRequestHeaders.Authorization.Parameter` round-trip, the secure cleartext never appearing in connection/Adapter rendering or any caught error message, the empty-SecureString rejection, the mutual-exclusivity rejection, and the parameter declaration itself (`Get-Command Connect-EntitySyncVendor` returns `ParameterType = securestring` in the `LCAT` parameter set); the "Keeps cleartext out" test was switched from `'http://'` to `'https://'` because the HTTP form fails HTTPS validation before any connection is created, so the test was passing for the wrong reason; README and `docs/Connect-EntitySyncVendor.md` SYNTAX + Example 3 advertise the new parameter, and the about-topic help now explicitly names `-LCATSecureBearer` and the mutual-exclusivity rule alongside the existing redaction guarantees; `just build`, `just test-load`, and `just test` pass (145 tests, 0 failed).
- Empty Queue sweep done: `NCentralEntityAdapter` now mirrors HaloPSA's rate-limit/backoff pattern — every `httpClient.SendAsync` call site (REST authenticate via `EnsureAccessTokenAsync`, REST reads/writes via `SendAuthenticatedAsync`, and the SOAP `InvokeEi2MethodAsync` EI2 path) goes through a new `SendWithRateLimitAsync` helper backed by a `SemaphoreSlim(1,1)`-guarded 500 ms minimum interval throttle, with up to `MaxRateLimitRetries = 6` retries on `429 TooManyRequests` honoring the `Retry-After` header (delta-seconds first, then future-date, falling through to `Math.Min(300, 15 * 2^attempt)`); the user API token rides each retry of the authenticate flow and the derived access token rides each retry of the subsequent REST calls, with `Dispose()` now releasing the throttle semaphore; Pester coverage mirrors the HaloPSA suite with six new tests — one 429 on authenticate then 200 (proves user API token rides both authenticate retries and the derived access token rides validate), one 429 on validate then 200 (proves the access token rides both validate attempts after a clean auth), seven consecutive 429s on authenticate (proves the adapter stops after `MaxRateLimitRetries`, raises `InvalidOperationException` with "HTTP 429", never leaks the user API token into the raised exception or the Out-String surface), and three reflection-driven `RateLimitDelay` tests for the delta-seconds / future-date / past-date-falls-through-to-exponential branches plus the `Math.Min(300, 15 * 2^attempt)` ladder for attempts 0..7; `just build` and `just test` pass (151 tests, +9s suite budget for the 6s exhaustion test).
- Empty Queue sweep done: `NetSuiteEntityAdapter` now mirrors HaloPSA/N-central's rate-limit/backoff pattern — every `httpClient.SendAsync` call site (`GetEntitiesAsync`, `TestConnectionAsync`, and the shared `ExecuteSuiteQlAsync` helper used for both entity reads and address enrichment) goes through a new `SendWithRateLimitAsync` helper backed by a `SemaphoreSlim(1,1)`-guarded 500 ms minimum interval throttle, with up to `MaxRateLimitRetries = 6` retries on `429 TooManyRequests` honoring the `Retry-After` header (delta-seconds first, then future-date, falling through to `Math.Min(300, 15 * 2^attempt)`); the OAuth header is re-signed per retry (fresh nonce + signature, since `BuildOAuthHeader` is invoked inside the factory closure) so NetSuite could honor each retry if the limit cleared mid-loop, and `Dispose()` now releases the throttle semaphore; Pester coverage mirrors the HaloPSA/N-central suite with five new tests — one 429 on the SuiteQL probe then 200 (proves `oauth_consumer_key`/`oauth_token` ride both attempts and the extracted `oauth_nonce`/`oauth_signature` strings differ across attempts, proving the factory closure re-runs), seven consecutive 429s on `TestConnectionAsync` (proves the adapter stops after `MaxRateLimitRetries`, returns `false` on the final 429, every retry carries `oauth_consumer_key`/`oauth_token`, and all 7 extracted `oauth_nonce` values are unique), and three reflection-driven `RateLimitDelay` tests for the delta-seconds / future-date / past-date-falls-through-to-exponential branches plus the `Math.Min(300, 15 * 2^attempt)` ladder for attempts 0..7; `just build` and `just test` pass (156 tests, +5 from 151, +9s suite budget for the 6s exhaustion test).
- Empty Queue sweep done: `en-US/about_LISSTech.EntitySync.help.txt` N-central CONFIGURATION paragraph rewritten to match the actual code contract (was claiming `NCENTRAL_USERNAME`/`NCENTRAL_PASSWORD`/`NCENTRAL_REGISTRATION_TOKEN` and an OAuth derivation that does not exist in `src/`); the new paragraph names `NCENTRAL_USER_API_TOKEN`/`NCENTRAL_SERVICE_ORG_ID` for REST and `NCENTRAL_SOAP_USERNAME`/`NCENTRAL_SOAP_PASSWORD` for the optional EI2 SOAP custom-property writes, and explains the `/api/auth/authenticate` flow rather than a fictional OAuth derivation; SEE ALSO now lists all 12 exported cmdlets (was 3 of 12); two new Pester tests lock in the contract — `About topic lists every exported cmdlet in SEE ALSO` matches each cmdlet name against the rendered help text, and `About topic N-central paragraph uses the real env-var contract` requires `NCENTRAL_USER_API_TOKEN`/`NCENTRAL_SERVICE_ORG_ID`/`NCENTRAL_SOAP_USERNAME` to be present while banning the bogus `NCENTRAL_USERNAME`/`NCENTRAL_PASSWORD`/`NCENTRAL_REGISTRATION_TOKEN` and the `"OAuth access token"` claim; `just build`, `just test-load`, and `just test` pass (158 tests, +2 from 156).
- Empty Queue sweep done: HaloPSA, N-central, and NetSuite adapters no longer each carry a byte-for-byte identical private `RateLimitDelay(HttpResponseMessage, int)` helper; the three duplicates (and the matching nine Pester reflection tests that poked at them on each adapter type) now share a single `RateLimitHelper.RateLimitDelay` in `src/Adapters/RateLimitHelper.cs` (Retry-After delta-seconds → future-date → `Math.Min(300, 15 * 2^attempt)` ladder, unchanged behaviour); the helper is `public static` so Pester's `[LISSTech.EntitySync.Adapters.RateLimitHelper].GetMethod('RateLimitDelay', 'Public, Static')` resolves it, and the adapter call sites read `var delay = RateLimitHelper.RateLimitDelay(response, attempt)`; `just build`, `just test-load`, and `just test` pass (158 tests, no test count change — the same nine branches are now exercised once per branch against the shared helper).
- Empty Queue sweep done: operator-facing stale text rewritten — `Module/LISSTech.EntitySync.psd1` `PSData.ReleaseNotes` (visible on PSGallery and via `Find-Module`) no longer claims "Initial PowerShell 7 entity sync module with NetSuite and HaloPSA adapters" but instead names all four shipped vendors (NetSuite, HaloPSA, N-central, LCAT), the shared `RateLimitHelper`-backed 429 retry policy (Retry-After delta-seconds/future-date with `Math.Min(300, 15 * 2^attempt)` fallback, 500ms inter-request throttle, up to six retries), the credential-redaction guarantee across plan artifacts / connection output / adapter error paths, and the `-LCATSecureBearer` SecureString entry point; README's `Connect-EntitySyncVendor` PowerShell API row now lists all four vendors instead of "NetSuite or HaloPSA"; `docs/Invoke-EntitySyncPlan.md` (the only one of the 12 `docs/*.md` files missing a `## SYNTAX` block — flagged in the previous reconnaissance) gains one listing `-Plan`/`-Apply`/`-PassThru`/`-TargetCustomFieldName` and the pipeline-by-value note; a new Pester test ("Manifest ReleaseNotes describes every shipped vendor and the rate-limit/bearer-redaction guarantees") loads the psd1 via `Import-PowerShellDataFile` and locks in the four vendor names plus the `Retry-After` / `redacted` / `SecureString` keywords so the field cannot drift back to "NetSuite and HaloPSA only" without a test failure; `just build`, `just test-load`, and `just test` pass (159 tests, +1 from 158).
- Empty Queue sweep done: remaining 4-vendor operator-facing stale text swept — README.md opening tagline now lists all four vendors (`NetSuite, HaloPSA, N-central, and LCAT` instead of `NetSuite and HaloPSA`); README.md badge row gained `N-central` and `LCAT` adapter badges alongside the existing `NetSuite`/`HaloPSA`/`Safe By Default` rows; README.md Architecture Mermaid diagram now draws four adapter boxes (`C` NetSuite, `D` HaloPSA, `K` N-central orange classDef, `L` LCAT red2 classDef) all feeding the shared `ExternalEntity` canonical model, and the Architecture table's `Adapters` row now says `NetSuite, HaloPSA, N-central, and LCAT specifics live at the edge`; `Module/LISSTech.EntitySync.psd1` `PSData.Tags` now includes `ncentral` alongside the existing `netsuite`/`halopsa`/`lcat` vendor tags so PowerShell Gallery discovery surfaces all four adapters; `docs/Get-EntitySyncConnection.md` Example 1 now registers all four vendors and the description no longer limits itself to "NetSuite and HaloPSA adapters"; a new Pester test ("Manifest PSData.Tags advertises every shipped vendor for PowerShell Gallery discovery") loads the psd1 via `Import-PowerShellDataFile` and locks in `netsuite`/`halopsa`/`ncentral`/`lcat` so the vendor tag list cannot silently drop N-central again; `just build` and `just test` pass (160 tests, +1 from 159).
- Empty Queue sweep done: HaloPSA, N-central, and NetSuite adapters no longer each carry a byte-for-byte identical private `SendWithRateLimitAsync` + `WaitForRequestSlotAsync` + `requestThrottle` semaphore + `nextRequestAt` field + `MinimumRequestIntervalMs`/`MaxRateLimitRetries` constants; the three duplicates now share a single `RateLimitedHttpRequester` in `src/Adapters/RateLimitedHttpRequester.cs` (Retry-After delta-seconds → future-date → `Math.Min(300, 15 * 2^attempt)` ladder via `RateLimitHelper.RateLimitDelay`, 500 ms minimum inter-request throttle via a `SemaphoreSlim(1,1)`-guarded `nextRequestAt`, up to 6 retries on 429, factory closure re-runs on every retry so per-attempt headers like a freshly signed OAuth signature are not cached); the helper is `public sealed` so Pester can resolve its surface via `[LISSTech.EntitySync.Adapters.RateLimitedHttpRequester].GetMethod(...)`, and `Dispose()` is the one place that releases the throttle semaphore — which fixes a pre-existing leak in `HaloEntityAdapter.Dispose()` that never released its `requestThrottle`; new Pester coverage locks in the constants (500 ms, 6 retries), the public `SendAsync(HttpClient, Func<HttpRequestMessage>, Action<string>, CancellationToken)` signature, the `IDisposable` contract, and a reflection check on each adapter type that proves `MinimumRequestIntervalMs`/`MaxRateLimitRetries`/`requestThrottle`/`nextRequestAt`/`SendWithRateLimitAsync`/`WaitForRequestSlotAsync` are no longer defined on the adapters (the behaviour is now exercised once on the shared helper instead of three times on the adapters); `just build`, `just test-load`, and `just test` pass (163 tests, +3 from 160).
- Empty Queue sweep done: residual 4-vendor operator-facing stale text swept that the previous stale-text sweeps (CHRONICLE entries 44, 45) missed — `en-US/about_LISSTech.EntitySync.help.txt` CONFIGURATION now documents NetSuite OAuth 1.0a credentials alongside the existing HaloPSA/N-central/LCAT paragraphs (covering `NETSUITE_ACCOUNT_ID`/`NETSUITE_CONSUMER_KEY`/`NETSUITE_CONSUMER_SECRET`/`NETSUITE_TOKEN_ID`/`NETSUITE_TOKEN_SECRET` plus the "fresh nonce and HMAC-SHA1 signature per attempt" guarantee and the source-only framing that mirrors the LCAT target-only paragraph), README.md "🧾 Status" section no longer uses the "Initial target / Also implemented" framing and instead treats all four adapters as equals with an explicit NetSuite→HaloPSA / HaloPSA→N-central / N-central→LCAT breakdown that names the source-only and target-only vendors, `src/Adapters/RateLimitHelper.cs` and `RateLimitedHttpRequester.cs` XML docs and inline comments now state explicitly that LCAT is intentionally excluded because its customer-scope write endpoint does not require outbound HTTP (the previous "HaloPSA, N-central, and NetSuite" phrasing made it sound like LCAT was missing the helpers by accident), the two test It-blocks that carried the same 3-of-4 phrasing were renamed to "the three HTTP-retry adapters (HaloPSA, N-central, NetSuite)" so the audit trail mirrors the source code, and four docs that were missing the `## SYNTAX` block (`docs/Get-EntitySyncLookup.md`, `docs/Invoke-EntitySyncChain.md`, `docs/New-EntitySyncPlan.md`, `docs/Set-EntitySyncCustomProperty.md`) now declare one derived from the parameter signatures in `src/Commands/*.cs` (including the dynamic `-Type` on Get-EntitySyncLookup, the two parameter sets on Invoke-EntitySyncChain, the dynamic source/target entity types on New-EntitySyncPlan, and the `-Id`/`-Name` aliases on Set-EntitySyncCustomProperty); two new Pester tests lock in the contract — "About topic CONFIGURATION section documents every shipped vendor" splits the rendered `Get-Help` output into the CONFIGURATION section and asserts each of NetSuite/HaloPSA/N-central/LCAT appears with its primary env var so a future sweep cannot drop one of the four paragraphs again, and "Every cmdlet doc has a SYNTAX block" walks every `docs/*.md` file and requires `^## SYNTAX$` so the remaining 4-of-12 SYNTAX gap cannot reopen; `just build`, `just test-load`, and `just test` pass (165 tests, +2 from 163).
- Empty Queue sweep done: NetSuite OAuth signature-method doc drift fixed — `en-US/about_LISSTech.EntitySync.help.txt:31` previously claimed "HMAC-SHA1 signature per attempt" but `src/Adapters/NetSuite/NetSuiteEntityAdapter.cs:382,401,411` actually use `HMAC-SHA256` (the `oauth_signature_method` is set to `"HMAC-SHA256"` and the signature is computed via `HMACSHA256` over the signing key); a new Pester test "About topic NetSuite paragraph names the real OAuth signature method" asserts `Get-Help about_LISSTech.EntitySync -Full` contains `HMAC-SHA256` and never contains `HMAC-SHA1`, so a future drift in either direction will fail with a focused error; `just test` passes (166 tests, +1 from 165).

## Open Findings
|----------|---------|----------|--------|
| follow-up | `EntitySyncPlanArtifactSanitizer.IsSensitiveName` uses a substring `Contains` match (`authorization|bearer|credential|password|secret|token`) that silently rewrites legitimate reviewer notes such as "tokenization policy review" or "password reset pending" into `[credential redacted]`; affects reason strings plus `ExternalIds`/`CustomFields` keys | `src/Core/EntitySyncPlanArtifactSanitizer.cs:7-15,51-54` | Resolved by empty queue sweep above (matcher is now identifier-boundary aware) |
| follow-up | 5 exported cmdlets have no `docs/*.md` (no external help): `Get-EntitySyncConnection`, `Test-EntitySyncConnection`, `Invoke-EntitySyncNetSuiteSuiteQL`, `Export-EntitySyncPlan`, `Import-EntitySyncPlan` | `Module/LISSTech.EntitySync.psd1:12-25` vs `docs/` | Resolved by empty queue sweep above |
| follow-up | Halo/NetSuite/NCentral adapters have minimal direct Pester coverage for `TestConnectionAsync`, rate-limit/backoff, and lookups | largest source files; recent LCAT `TestConnectionAsync` work (commit `40244f3`) is unmirrored | Resolved for `TestConnectionAsync` and `GetLookupsAsync` across all three vendors (HaloPSA and N-central in commit `eb176d3` and the lookups sweep; NetSuite in the TestConnection sweep); Halo rate-limit/backoff is covered (`0825cbd`); N-central rate-limit/backoff is covered by the current sweep (REST and SOAP paths, plus `RateLimitDelay` reflection); NetSuite rate-limit/backoff is covered by the current sweep (SuiteQL probe retries with re-signed OAuth header, plus three `RateLimitDelay` reflection tests); the row stays in the table to record the audit trail |
| follow-up | `docs/New-EntitySyncPlan.md:62` and both copies of `LISSTech.EntitySync.dll-Help.xml:338` advertise `MatchType 'LcatSiteParentMissing'` for the missing-parent-N-central-customer safe-failure case, but `src/Commands/NewEntitySyncPlanCommand.cs` emits a unified `MatchType = "LcatSourceInvalid"` for every LCAT-target safe-failure (missing parent ID, missing source ID, duplicate source ID, duplicate slug, non-NCentral source, unsafe slug) and the tests at lines 2000, 3200, 3238 (plus reflection at 2777, 2815, 3546, 3603, 3625) assert `LcatSourceInvalid`; the doc/help XML needs to be updated to match the code (or vice versa) | `docs/New-EntitySyncPlan.md:62`, `en-US/LISSTech.EntitySync.dll-Help.xml:338`, `Module/en-US/LISSTech.EntitySync.dll-Help.xml:338` vs `src/Commands/NewEntitySyncPlanCommand.cs:186-195` | Open |

## Decisions To Preserve

-
